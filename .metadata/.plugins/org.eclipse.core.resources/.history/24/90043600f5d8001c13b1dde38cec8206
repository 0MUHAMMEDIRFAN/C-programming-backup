#include <stdio.h>
#include <stdlib.h>
void sum();
int main(void)
{
	int a,b,x;
	setbuf(stdout,NULL);
	printf("enter");
	scanf("%d %d",&a,&b);
	sum(a,b);

	return EXIT_SUCCESS;
}

int sum (int a, int b){
	int s;
	s=a+b;
	return (main);
}
