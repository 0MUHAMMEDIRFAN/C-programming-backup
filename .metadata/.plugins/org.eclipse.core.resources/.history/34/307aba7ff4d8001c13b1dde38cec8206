#include <stdio.h>
#include <stdlib.h>
void sum();
int main(void)
{
	int a,b;
	setbuf(stdout,NULL);
	printf("enter");
	scanf("%d %d",&a,&b);
	sum(a,b);
	return EXIT_SUCCESS;
}

void sum (int a, int b){
	int s,x,y;
	scanf("%d %d",&x,&y);
	s=a+b;
	printf("%d",s);
}
