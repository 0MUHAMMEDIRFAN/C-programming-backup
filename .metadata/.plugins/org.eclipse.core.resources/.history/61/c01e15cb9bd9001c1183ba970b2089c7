#include <stdio.h>
#include <stdlib.h>

int main(void) {
	int a,x,sum=0;
	setbuf(stdout,NULL);
	puts("\n \t give me a limit");
	scanf("%d",&a);
	for(x=1;x<=a;x=x+2)
	{
		sum=sum+x;
		printf("\n\t sm of odd number = %d",sum);
	}
	return EXIT_SUCCESS;
}
